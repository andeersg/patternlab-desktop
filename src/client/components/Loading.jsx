import React, { Component } from 'react';

class Loading extends Component {
  render() {
    return (
      <section className="loading">
        <h1 className="loading__title">Loading application</h1>
      </section>
    );
  }
}

export default Loading;
